// src/pages/Auth.jsx — thin wrapper; AuthCard owns its own full-page layout
import React from "react";
import AuthCard from "../components/auth/AuthCard";

export default function AuthPage() {
  return <AuthCard />;
}
